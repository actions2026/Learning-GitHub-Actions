apiVersion: v1
kind: Workflow
metadata:
  name: thinknyxapp-svc
  labels:
    app: thinknyxapp
spec:
  type: NodePort
  selector:
    app: thinknyxapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30007
